   73  # Add Docker's official GPG key:
   74  sudo apt-get update
   75  sudo apt-get install ca-certificates curl
   76  sudo install -m 0755 -d /etc/apt/keyrings
   77  sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
   78  sudo chmod a+r /etc/apt/keyrings/docker.asc
   79  # Add the repository to Apt sources:
   80  echo   "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" |   sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
   81  sudo apt-get update
   82  sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
   83  sudo docker run hello-world
   84  ls
   85  less SlurmVC_SimRepo.Dockerfile 
   86  nano SlurmVC_SimRepo.Dockerfile 
   87  make dockerimage
   88  ls
   89  cd /scratchlocal/tutorial/slurm_model/
   90  ls
   91  ls epyc
   92  cd /scratchlocal/
   93  git clone https://github.com/ubccr-slurm-simulator/slurm_model.git
   94  ls
   95  cd slurm_model/
   96  ls
   97  cd ..
   98  rm -rf slurm_model/
   99  git clone https://github.com/ubccr-slurm-simulator/slurm_model.git
  100  export INSTALL_PREFIX=/scratchlocal/slurm_model/epyc
  101  cd slurm_model/
  102  mv .dockerignore .dockerignore_bkp-1
  103  systemctl start docker
  104  docker pull centos:7
  105  ls
  106  docker build -f /scratchlocal/tutorial/slurm_model/hpc-digital-twin/utilities/SlurmVC_SimRepo.Dockerfile -t carc/slurm_vc:slurm-23-02-sim .
  107  ls
  108  cd ..
  109  ls
  110  cd tutorial/
  111  ls
  112  cd slurm_model/hpc-digital-twin/utilities/
  113  ls
  114  make dockerfile
  115  make dockerimage
